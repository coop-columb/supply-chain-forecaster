# Environment (development, production)
ENV=development

# API configuration
API_HOST=0.0.0.0
API_PORT=8000
API_DEBUG=True

# Dashboard configuration
DASHBOARD_HOST=0.0.0.0
DASHBOARD_PORT=8050
DASHBOARD_DEBUG=True

# Database connection (for future use)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=supply_chain
DB_USER=postgres
DB_PASSWORD=postgres

# Model configuration
MODEL_TYPE=prophet
MODEL_VERSION=latest
MODEL_STORAGE_PATH=models/deployment